import java.util.Random;

public class Driver {

	public static void main(String[] args) {
		
		BinaryTree<Integer> bt = new BinaryTree<>();
		Random rand = new Random();
		for(int i = 0; i < 16; i++){
			bt.insert(rand.nextInt(i));
		}
		
		BTreePrinter.printNode(bt.getRoot());
		
	}

}
