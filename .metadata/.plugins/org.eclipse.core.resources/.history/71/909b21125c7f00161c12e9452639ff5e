
public class A {

	public void minMax() {
		int max = min = num.nextInt();
		while (num.hasNextInt()) {
			int newNum = num.nextInt();

			if (newNum > max)
				max = newNum;

			if (newNum < min)
				min = newNum;
		}
	}

}
